include_rules

src = \
    src/main.c \
    src/dynamic_array.c \
    src/mathparse.c

CFLAGS = -g
LDFLAGS = -g -L./out
LDLIBS = -lm

: foreach $(src) |> !cc |> out/%B.o
: out/*.o |> !ld |> out/test

.gitignore
